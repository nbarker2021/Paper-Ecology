"""
Paper 25 — Unified Energetic Traversal Maps (redirect)
======================================================

This file is deprecated.  The real verifier has been moved to:

    paper-25__energetic_traversal_verifier.py

That module is self-contained, implements NSL boundary accounting,
energetic traversal maps, LCR-based pathfinding, critical point
detection, and the VOA 2+6 sector split, with no external dependencies.

All B-family references have been stripped.  Use A-P025-CODE.
"""

if __name__ == '__main__':
    print("Paper 25 stub: redirect to paper-25__energetic_traversal_verifier.py")
