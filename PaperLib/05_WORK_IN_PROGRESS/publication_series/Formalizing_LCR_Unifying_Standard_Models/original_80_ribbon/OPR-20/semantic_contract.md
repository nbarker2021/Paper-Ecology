# Semantic Contract - OPR-20

## State Transition

`Layer 2 Synthesis Ledger` explains why the ribbon needs a `closure_lift` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `10`  
Next lift: `30`
