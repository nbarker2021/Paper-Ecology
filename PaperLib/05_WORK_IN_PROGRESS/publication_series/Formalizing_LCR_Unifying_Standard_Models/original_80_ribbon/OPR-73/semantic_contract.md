# Semantic Contract - OPR-73

## State Transition

`Fold And Representation Surface At Lift 7` explains why the ribbon needs a `fold_action` action at lift depth
`7`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `63`  
Next lift: `terminal/open`
