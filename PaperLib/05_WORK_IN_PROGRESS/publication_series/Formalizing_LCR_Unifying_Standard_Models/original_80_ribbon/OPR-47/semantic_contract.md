# Semantic Contract - OPR-47

## State Transition

`Bridge And Projection Surface At Lift 4` explains why the ribbon needs a `bridge_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `37`  
Next lift: `57`
