# Semantic Contract - OPR-52

## State Transition

`Residual And Correction Surface At Lift 5` explains why the ribbon needs a `residual_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `42`  
Next lift: `62`
