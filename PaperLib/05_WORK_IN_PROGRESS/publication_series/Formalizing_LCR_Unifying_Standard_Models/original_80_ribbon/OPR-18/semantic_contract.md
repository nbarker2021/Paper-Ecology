# Semantic Contract - OPR-18

## State Transition

`VOA Moonshine Representation Routes` explains why the ribbon needs a `terminal_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `08`  
Next lift: `28`
