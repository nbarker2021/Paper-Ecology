# Semantic Contract - OPR-48

## State Transition

`Terminal Address Surface At Lift 4` explains why the ribbon needs a `terminal_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `38`  
Next lift: `58`
