# Semantic Contract - OPR-71

## State Transition

`Enumeration Action At Lift 7` explains why the ribbon needs a `enumeration_action` action at lift depth
`7`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `61`  
Next lift: `terminal/open`
