# Semantic Contract - OPR-14

## State Transition

`GR Boundary Repair Curvature` explains why the ribbon needs a `boundary_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `04`  
Next lift: `24`
