# Semantic Contract - OPR-49

## State Transition

`Window And Meta-Readout At Lift 4` explains why the ribbon needs a `window_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `39`  
Next lift: `59`
