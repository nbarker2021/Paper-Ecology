# Semantic Contract - OPR-26

## State Transition

`Z Pinch and Shear Horizon` explains why the ribbon needs a `ledger_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `16`  
Next lift: `36`
