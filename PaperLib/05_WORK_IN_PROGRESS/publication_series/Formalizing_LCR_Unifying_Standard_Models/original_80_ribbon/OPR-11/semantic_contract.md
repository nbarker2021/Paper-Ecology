# Semantic Contract - OPR-11

## State Transition

`Theory Admission Gate` explains why the ribbon needs a `enumeration_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `01`  
Next lift: `21`
