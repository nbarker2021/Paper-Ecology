# Semantic Contract - OPR-59

## State Transition

`Window And Meta-Readout At Lift 5` explains why the ribbon needs a `window_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `49`  
Next lift: `69`
