# Semantic Contract - OPR-29

## State Transition

`Monster Universal Energy Bound Hypotheses` explains why the ribbon needs a `window_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `19`  
Next lift: `39`
