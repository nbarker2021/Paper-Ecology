# Semantic Contract - OPR-68

## State Transition

`Terminal Address Surface At Lift 6` explains why the ribbon needs a `terminal_action` action at lift depth
`6`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `58`  
Next lift: `78`
