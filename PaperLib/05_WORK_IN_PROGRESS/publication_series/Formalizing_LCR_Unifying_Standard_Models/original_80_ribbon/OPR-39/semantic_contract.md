# Semantic Contract - OPR-39

## State Transition

`Window And Meta-Readout At Lift 3` explains why the ribbon needs a `window_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `29`  
Next lift: `49`
