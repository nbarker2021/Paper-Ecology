# Semantic Contract - OPR-38

## State Transition

`Terminal Address Surface At Lift 3` explains why the ribbon needs a `terminal_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `28`  
Next lift: `48`
