# Semantic Contract - OPR-17

## State Transition

`E6 E8 Error Correction Tower` explains why the ribbon needs a `bridge_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `07`  
Next lift: `27`
