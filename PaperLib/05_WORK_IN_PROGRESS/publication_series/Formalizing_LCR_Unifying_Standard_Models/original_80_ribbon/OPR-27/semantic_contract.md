# Semantic Contract - OPR-27

## State Transition

`Observer Delay and Shared Reality` explains why the ribbon needs a `bridge_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `17`  
Next lift: `37`
