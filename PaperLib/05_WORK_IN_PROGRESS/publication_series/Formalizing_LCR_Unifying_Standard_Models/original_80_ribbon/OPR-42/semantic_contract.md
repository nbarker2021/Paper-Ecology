# Semantic Contract - OPR-42

## State Transition

`Residual And Correction Surface At Lift 4` explains why the ribbon needs a `residual_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `32`  
Next lift: `52`
