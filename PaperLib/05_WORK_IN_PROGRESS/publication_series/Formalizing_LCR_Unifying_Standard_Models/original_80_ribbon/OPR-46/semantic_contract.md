# Semantic Contract - OPR-46

## State Transition

`Ledger And Causal Binding At Lift 4` explains why the ribbon needs a `ledger_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `36`  
Next lift: `56`
