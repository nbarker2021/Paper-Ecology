# Semantic Contract - OPR-16

## State Transition

`Continuum Edge Residuals` explains why the ribbon needs a `ledger_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `06`  
Next lift: `26`
