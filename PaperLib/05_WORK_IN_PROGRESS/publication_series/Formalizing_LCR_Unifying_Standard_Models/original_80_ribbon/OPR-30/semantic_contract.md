# Semantic Contract - OPR-30

## State Transition

`Grand Ribbon Meta Framer` explains why the ribbon needs a `closure_lift` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `20`  
Next lift: `40`
