# Semantic Contract - OPR-50

## State Transition

`Ten-Window Closure And Lift At Lift 5` explains why the ribbon needs a `closure_lift` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `40`  
Next lift: `60`
