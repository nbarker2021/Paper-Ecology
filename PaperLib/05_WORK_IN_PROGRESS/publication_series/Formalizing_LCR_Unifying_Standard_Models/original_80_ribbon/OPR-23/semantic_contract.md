# Semantic Contract - OPR-23

## State Transition

`FoldForge Protein Folding` explains why the ribbon needs a `fold_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `13`  
Next lift: `33`
