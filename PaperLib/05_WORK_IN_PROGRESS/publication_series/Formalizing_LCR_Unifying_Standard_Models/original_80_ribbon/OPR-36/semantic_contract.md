# Semantic Contract - OPR-36

## State Transition

`Ledger And Causal Binding At Lift 3` explains why the ribbon needs a `ledger_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `26`  
Next lift: `46`
