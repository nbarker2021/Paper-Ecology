# Semantic Contract - OPR-55

## State Transition

`Carrier And Transport Surface At Lift 5` explains why the ribbon needs a `carrier_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `45`  
Next lift: `65`
