# Semantic Contract - OPR-66

## State Transition

`Ledger And Causal Binding At Lift 6` explains why the ribbon needs a `ledger_action` action at lift depth
`6`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `56`  
Next lift: `76`
