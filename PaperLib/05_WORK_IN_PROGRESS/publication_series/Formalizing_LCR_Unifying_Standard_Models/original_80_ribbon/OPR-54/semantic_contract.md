# Semantic Contract - OPR-54

## State Transition

`Boundary Repair Surface At Lift 5` explains why the ribbon needs a `boundary_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `44`  
Next lift: `64`
