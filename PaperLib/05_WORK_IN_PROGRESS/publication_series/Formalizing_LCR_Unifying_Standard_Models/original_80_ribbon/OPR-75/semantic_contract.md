# Semantic Contract - OPR-75

## State Transition

`Carrier And Transport Surface At Lift 7` explains why the ribbon needs a `carrier_action` action at lift depth
`7`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `65`  
Next lift: `terminal/open`
