# Semantic Contract - OPR-51

## State Transition

`Enumeration Action At Lift 5` explains why the ribbon needs a `enumeration_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `41`  
Next lift: `61`
