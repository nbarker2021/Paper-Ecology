# Semantic Contract - OPR-04

## State Transition

`Boundary Repair` explains why the ribbon needs a `boundary_action` action at lift depth
`0`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `none`  
Next lift: `14`
