# Semantic Contract - OPR-13

## State Transition

`Standard Model Quark Face Transport` explains why the ribbon needs a `fold_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `03`  
Next lift: `23`
