# Semantic Contract - OPR-65

## State Transition

`Carrier And Transport Surface At Lift 6` explains why the ribbon needs a `carrier_action` action at lift depth
`6`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `55`  
Next lift: `75`
