# Semantic Contract - OPR-41

## State Transition

`Enumeration Action At Lift 4` explains why the ribbon needs a `enumeration_action` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `31`  
Next lift: `51`
