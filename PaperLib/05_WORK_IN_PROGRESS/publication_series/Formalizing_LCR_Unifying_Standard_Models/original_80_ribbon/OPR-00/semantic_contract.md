# Semantic Contract - OPR-00

## State Transition

`Established Grounding and Axiom Contract` explains why the ribbon needs a `closure_lift` action at lift depth
`0`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `none`  
Next lift: `10`
