# Paper 18 Internal Reasoning Supplement

**Attachment type:** internally supplied reasoning.  
**Method note:** reasoning and possible uses only; NSIT/grading owns promotion.

## Core Reading

Paper 18 provides a finite VOA/Moonshine route seed. The useful reasoning is to
treat it as a representation-route architecture with bounded table classes,
not as automatic full Moonshine.

## Reasoning Additions

| Addition | Use for this paper |
|----------|--------------------|
| Seed vs identification | A finite VOA seed can be real route data without being the global Moonshine theorem. |
| Character/partition bookkeeping | `2q^0 + 6q^5` can be stored as a finite character-like accounting object. |
| Static template caution | Static `Z4` periods are coordinate-template periods, not temporal CA periods unless a temporal theorem is attached. |
| McKay class registry | Hardcoded classes `1A,2A,3A,5A,7A` should be represented as a bounded table registry. |
| Correction-to-class hypothesis | Assigning correction coordinates to Moonshine classes is a route hypothesis until a binding theorem exists. |
| Monster scalar anchor | `196883 = 47*59*71` can be shared with Paper 29 as scalar registration data. |

## Possible Uses

1. Build a McKay class registry keyed by class, coefficient/source table, route
   status, and receipt.
2. Store `Z(q)` sector split as a CAM query object for Papers 15/29.
3. Write a static-vs-temporal template guard shared with observer papers.
4. Route correction-via-VOA as a concrete table-binding theorem target.

## Deferred Back-Application Slots

| Slot | Current local reading | Later source allowed to refine it | What may change | What must remain invariant | Trigger |
|------|-----------------------|-----------------------------------|-----------------|----------------------------|---------|
| `18.BA.1` | VOA seed is finite sector data. | Papers 15, 29 and NP-01. | Later work may bind sector data to correction collapse. | Finite sector split remains distinct from full Moonshine. | VOA/correction theorem or table receipt. |
| `18.BA.2` | McKay bootstrap is bounded by declared classes. | Papers 09, 12, 29 and NP-01. | Later classes or unbounded formulas may be added. | Class/source table must remain explicit. | McKay table expansion or theorem. |
| `18.BA.3` | Static Z4 template is not temporal periodicity. | Papers 19, 27 and NP-10. | Later observer work may reuse the template. | Temporal claims need temporal traces. | Temporal verifier or counterexample suite. |

## NSIT Questions To Hand Off

| Question | Why it matters |
|----------|----------------|
| Which Moonshine table/class entries are actually attached? | Makes bounded bootstrap queryable. |
| What would prove correction-via-VOA? | Turns a route into a theorem target. |
| How do static representation periods differ from temporal CA periods? | Prevents observer/period overclaim. |

