# Semantic Contract - OPR-24

## State Transition

`KnightForge N Dimensional Chess Automata` explains why the ribbon needs a `boundary_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `14`  
Next lift: `34`
