# Semantic Contract - OPR-69

## State Transition

`Window And Meta-Readout At Lift 6` explains why the ribbon needs a `window_action` action at lift depth
`6`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `59`  
Next lift: `79`
