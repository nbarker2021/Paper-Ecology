# Semantic Contract - OPR-57

## State Transition

`Bridge And Projection Surface At Lift 5` explains why the ribbon needs a `bridge_action` action at lift depth
`5`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `47`  
Next lift: `67`
