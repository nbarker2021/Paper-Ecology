# Semantic Contract - OPR-32

## State Transition

`The Supervisor Cursor` explains why the ribbon needs a `residual_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `22`  
Next lift: `42`
