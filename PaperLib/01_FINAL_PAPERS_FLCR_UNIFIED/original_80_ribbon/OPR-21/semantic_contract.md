# Semantic Contract - OPR-21

## State Transition

`MorphForge PolyForge MorphoniX` explains why the ribbon needs a `enumeration_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `11`  
Next lift: `31`
