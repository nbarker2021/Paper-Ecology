# Semantic Contract - OPR-76

## State Transition

`Ledger And Causal Binding At Lift 7` explains why the ribbon needs a `ledger_action` action at lift depth
`7`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `66`  
Next lift: `terminal/open`
