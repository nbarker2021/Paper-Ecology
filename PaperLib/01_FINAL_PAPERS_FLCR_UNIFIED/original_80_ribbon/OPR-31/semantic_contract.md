# Semantic Contract - OPR-31

## State Transition

`It Was Still Just LCR` explains why the ribbon needs a `enumeration_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `21`  
Next lift: `41`
