# Semantic Contract - OPR-33

## State Transition

`Fold And Representation Surface At Lift 3` explains why the ribbon needs a `fold_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `23`  
Next lift: `43`
