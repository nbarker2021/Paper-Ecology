# Semantic Contract - OPR-22

## State Transition

`MetaForge Applied Materials` explains why the ribbon needs a `residual_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `12`  
Next lift: `32`
