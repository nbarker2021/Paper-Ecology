# Semantic Contract - OPR-79

## State Transition

`Window And Meta-Readout At Lift 7` explains why the ribbon needs a `window_action` action at lift depth
`7`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `69`  
Next lift: `terminal/open`
