# Semantic Contract - OPR-40

## State Transition

`Ten-Window Closure And Lift At Lift 4` explains why the ribbon needs a `closure_lift` action at lift depth
`4`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `30`  
Next lift: `50`
