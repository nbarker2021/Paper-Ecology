# Semantic Contract - OPR-25

## State Transition

`Energetic Traversal Maps` explains why the ribbon needs a `carrier_action` action at lift depth
`2`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `15`  
Next lift: `35`
