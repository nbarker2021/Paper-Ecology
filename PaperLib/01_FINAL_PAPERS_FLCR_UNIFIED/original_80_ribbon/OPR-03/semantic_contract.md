# Semantic Contract - OPR-03

## State Transition

`D4 J3 Triality Surface` explains why the ribbon needs a `fold_action` action at lift depth
`0`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `none`  
Next lift: `13`
