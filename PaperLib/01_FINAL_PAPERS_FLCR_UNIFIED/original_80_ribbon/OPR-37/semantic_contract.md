# Semantic Contract - OPR-37

## State Transition

`Bridge And Projection Surface At Lift 3` explains why the ribbon needs a `bridge_action` action at lift depth
`3`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `27`  
Next lift: `47`
