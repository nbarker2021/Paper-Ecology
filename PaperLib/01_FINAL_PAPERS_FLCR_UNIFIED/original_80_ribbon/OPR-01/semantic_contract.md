# Semantic Contract - OPR-01

## State Transition

`LCR Chain Carrier` explains why the ribbon needs a `enumeration_action` action at lift depth
`0`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `none`  
Next lift: `11`
