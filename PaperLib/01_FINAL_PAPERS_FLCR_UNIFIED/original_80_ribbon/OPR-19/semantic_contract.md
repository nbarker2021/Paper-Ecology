# Semantic Contract - OPR-19

## State Transition

`Observer Face Selection` explains why the ribbon needs a `window_action` action at lift depth
`1`. The semantic layer connects the prior ribbon state to the produced
state while preserving the formal object named in the formal contract.

## Same-Family Context

Previous lift: `09`  
Next lift: `29`
